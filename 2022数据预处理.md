# 2022 数据预处理说明

本文档说明 `2022省级综合表.xlsx` 的数据预处理流程。预处理目标是将省级社会经济数据与省级碳排放清单合并，形成可用于后续建模分析的 2022 年省级综合数据表。

## 1. 数据来源

本次预处理使用两个 Excel 文件：

1. `数据/2022数据.xlsx`
   - 包含省级人口、第二产业、城镇化率和 GDP 数据。
   - 原始字段包括：`地  区`、`人口（万人）`、`第二产业（亿元）`、`城镇化`、`GDP（亿元）`。

2. `数据/附件2：2022 年全国 30 个省份碳排放清单.xlsx`
   - 包含 30 个省份的 2022 年碳排放清单。
   - 每个省份对应一个 sheet，sheet 名为英文省份名加年份，例如 `Beijing2022`。
   - `NOTE` sheet 为说明页，不参与数据提取。

## 2. 社会经济数据清洗

首先读取 `2022数据.xlsx`，并进行如下处理：

1. 删除整行为空的记录。
2. 清理地区名称中的空格，将类似 `北  京` 的名称统一为 `北京`。
3. 将字段重命名为统一格式：
   - `地  区` 重命名为 `省份`
   - `人口（万人）` 重命名为 `人口_万人`
   - `第二产业（亿元）` 重命名为 `第二产业_亿元`
   - `城镇化` 重命名为 `城镇化率_%`
   - `GDP（亿元）` 重命名为 `GDP_亿元`
4. 将人口、第二产业、城镇化率和 GDP 字段转换为数值型，便于后续计算。

## 3. 碳排放数据提取

对附件2中的省份 sheet 逐一读取，并跳过 `NOTE` sheet。

每个省份 sheet 中，只提取 `Emission_Inventory == "TotalEmissions"` 的总排放行。提取的字段包括：

- `Scope_1_Total`
- `Raw_Coal`
- `CleanedCoal`
- `Other_Washed_Coal`
- `Briquettes`
- `Coke`
- `Coke_Oven_Gas`
- `Other_Gas`
- `Other_Coking_Products`
- `Crude_Oil`
- `Gasoline`
- `Kerosene`
- `Diesel_Oil`
- `Fuel_Oil`
- `LPG`
- `Refinery_Gas`
- `Other_Petroleum_Products`
- `Natural_Gas`
- `Process`

其中，`Scope_1_Total` 被重命名为 `CO2总量_Mt`。同时根据英文 sheet 名与中文省份名的映射关系，为每条记录添加中文 `省份` 字段。

## 4. 能源结构指标构造

基于附件2中提取的排放字段，构造四类能源结构指标。

煤炭相关排放：

```text
煤炭相关排放_Mt =
Raw_Coal + CleanedCoal + Other_Washed_Coal + Briquettes + Coke
+ Coke_Oven_Gas + Other_Gas + Other_Coking_Products
```

石油相关排放：

```text
石油相关排放_Mt =
Crude_Oil + Gasoline + Kerosene + Diesel_Oil + Fuel_Oil
+ LPG + Refinery_Gas + Other_Petroleum_Products
```

天然气与过程排放：

```text
天然气排放_Mt = Natural_Gas
过程排放_Mt = Process
```

随后分别计算各类排放占 CO2 总量的比例：

```text
煤炭相关排放占比 = 煤炭相关排放_Mt / CO2总量_Mt
石油相关排放占比 = 石油相关排放_Mt / CO2总量_Mt
天然气排放占比 = 天然气排放_Mt / CO2总量_Mt
过程排放占比 = 过程排放_Mt / CO2总量_Mt
```

## 5. 数据合并

将社会经济数据和碳排放数据按 `省份` 字段进行合并。

由于附件2只包含 30 个省份，不包含西藏，因此合并时只保留两个数据源都存在的省份。最终主表保留 30 个省份，西藏被排除。

## 6. 综合指标构造

在合并后的主表中，进一步构造以下综合指标：

```text
人均CO2_吨每人 = CO2总量_Mt * 100 / 人口_万人
碳排放强度_吨每万元GDP = CO2总量_Mt * 100 / GDP_亿元
人均GDP_万元每人 = GDP_亿元 / 人口_万人
第二产业占比 = 第二产业_亿元 / GDP_亿元
技术效率_万元GDP每吨CO2 = 1 / 碳排放强度_吨每万元GDP
```

其中，`CO2总量_Mt * 100 / 人口_万人` 和 `CO2总量_Mt * 100 / GDP_亿元` 完成了单位换算，使结果分别对应“吨每人”和“吨每万元 GDP”。

## 7. 输出字段

最终主表按以下字段顺序输出：

1. `省份`
2. `CO2总量_Mt`
3. `人口_万人`
4. `GDP_亿元`
5. `第二产业_亿元`
6. `城镇化率_%`
7. `人均CO2_吨每人`
8. `碳排放强度_吨每万元GDP`
9. `人均GDP_万元每人`
10. `第二产业占比`
11. `技术效率_万元GDP每吨CO2`
12. `煤炭相关排放_Mt`
13. `煤炭相关排放占比`
14. `石油相关排放_Mt`
15. `石油相关排放占比`
16. `天然气排放_Mt`
17. `天然气排放占比`
18. `过程排放_Mt`
19. `过程排放占比`

## 8. 校验内容

输出文件中除主表外，还包含校验信息，主要用于确认数据合并和指标计算是否完整：

1. 最终主表行数。
2. `2022数据.xlsx` 中存在但附件2中不存在的省份。
3. 附件2中存在但 `2022数据.xlsx` 中不存在的省份。
4. 每列缺失值数量。
5. 人均 CO2、碳排放强度、第二产业占比以及能源排放占比的描述性统计。

本次校验结果显示：

- 最终主表行数为 30。
- `2022数据.xlsx` 中有但附件2没有的省份为西藏。
- 附件2中有但 `2022数据.xlsx` 没有的省份为无。
- 主表各字段缺失值数量均为 0。

## 9. 输出结果

最终生成文件为：

```text
数据/2022省级综合表.xlsx
```

该文件包含 `主表`、`省份匹配校验`、`列缺失值数量` 和 `描述性统计` 四个 sheet。
